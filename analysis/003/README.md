Procesamiento de [este tipo de archivos]
[con este objetivo].

Para ejecutar el procedimiento poner [este tipo de archivos] en el directorio `data/`
y ejecutar:

```
bin/targets | xargs mk
```

Los resultados deben generarse en `results/`
